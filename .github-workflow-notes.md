# Windows 构建与更新发布

实际工作流在 [.github/workflows/windows.yml](.github/workflows/windows.yml)。推送到 `main` 或手动运行工作流，会生成 Windows NSIS `*-setup.exe`，保存在 Actions Artifacts。推送 `vX.Y.Z` 标签会构建签名更新包、发布 GitHub Release，并上传 `latest.json` 供应用内“检查更新”按钮使用。[Tauri 官方说明](https://v2.tauri.app/distribute/pipelines/github/)列出了 `tauri-action` 的发布流程。

## 首次接入 GitHub

1. 把此目录推送到你自己的 GitHub 仓库。工作流在发布时读取 `github.repository`，因此仓库地址不写死在应用源码里。
2. 在 GitHub 仓库 `Settings → Secrets and variables → Actions` 中添加 `TAURI_SIGNING_PRIVATE_KEY`。值是本机 `.secrets/auto-music.key` **文件的完整内容**。此文件已经被 `.gitignore` 排除；妥善备份，切勿提交。公开验证密钥已经写入 `src-tauri/tauri.conf.json`。签名为 Tauri 更新器的必需条件，不能关闭；参见[官方更新文档](https://v2.tauri.app/plugin/updater/)。
3. 发布前同时修改 `package.json`、`src-tauri/Cargo.toml` 和 `src-tauri/tauri.conf.json` 的版本号。标签必须与它们相同，例如当前版本为 `v0.1.0`。发布脚本会验证一致性。
4. 推送发布标签：

   ```bash
   git tag v0.1.0
   git push origin main
   git push origin v0.1.0
   ```

首次发布后，从该 Release 安装的 Windows 程序会带有更新源。之后发布更高版本，点击“检查更新”即可下载、验证签名并安装。普通分支构建的 Artifact 只用于测试，不含发布更新源。

## 范围

更新器只更新桌面应用。`windows-vhid/driver` 中的内核驱动是独立安装包，需单独构建、签名和安装；应用更新不会替换驱动。Windows CI 与驱动签名状态需要在 GitHub 的 Windows runner 上验证。
